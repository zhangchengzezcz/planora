# Planora 1.7.10

## 中文
- 修复 1.7.9 中课程出勤仍显示“未记录”：ManageBac 返回的课表颜色可能使用 `color(srgb …)` 或 `color(display-p3 …)`，现已按其正确的数值范围解析。
- 在真实课表页面只读核对了颜色类别；新增回归测试覆盖绿色、橙色、红色、灰色，以及重新同步后纠正旧的“未记录”课程。
- 更新后请重新同步 ManageBac，旧课程状态会随同步更新。不会修改学校端记录。
- 39 项 ManageBac 测试通过。真实账户在新版 App 中的完整同步结果仍需核对。

## English
- Fixed lessons still showing "Not Recorded" in 1.7.9. ManageBac may return timetable colors as `color(srgb …)` or `color(display-p3 …)`; Planora now interprets their numeric ranges correctly.
- Checked color categories against the live timetable without changing school data. Regression tests cover green, orange, red, and gray lessons, plus correcting previously imported records on resync.
- Sync ManageBac again after updating to refresh existing lessons. No school records are changed.
- All 39 ManageBac tests passed. End-to-end sync in the updated app still needs live-account verification.

The macOS app is ad-hoc signed, not Developer ID signed or notarized.
macOS 应用使用临时签名，未进行 Developer ID 签名或公证。
